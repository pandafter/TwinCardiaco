[project]
name = "twin-cardiaco-backend"
version = "0.1.0"
description = "FastAPI realtime service for TwinCardiaco"
requires-python = ">=3.12"
dependencies = [
    "aiohttp>=3.9",
    "anthropic>=0.116",
    "fastapi[standard]>=0.128.1",
    "python-dotenv>=1.0",
]
